# --- Pipeline / batch scoring (scripts/prepare_features.py) ---
# scikit-learn PINNED to the exact version model.pkl was serialized with.
# Loading the artifact with a different version raises InconsistentVersionWarning
# and risks invalid results (see PLANNING.md section 7). Only the batch job needs this.
scikit-learn==1.8.0
pandas==2.3.3

# --- Serving / API (app/) ---
fastapi==0.121.0
uvicorn[standard]==0.38.0
prometheus-client==0.24.0

# --- Tests ---
pytest==9.0.1
httpx==0.28.1
